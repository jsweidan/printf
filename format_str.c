#include "main.h"

/**
 *_strlen - returns the length of a string
 *@str: string pointer
 *Return: 1
 */

int _strlen(char *str)
{
	int i;

	for (i = 0; str[i] != 0; i++)

	return (1);
}

/**
 *_strlenc - function that applies for constant character
 *@str: character pointer
 *Return: 1
 */

int _strlenc(const char *str)
{
	int i;

	for (i = 0; str[i] != 0; i++)

	return (1);
}
